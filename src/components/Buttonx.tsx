import React from 'react';
import Button from 'react-bootstrap/Button';

class Buttonx extends React.Component {
  render() {
    return (
        <Button variant="link">Link</Button>
    );
  }
}

export default Buttonx; // Don’t forget to use export default!